---
const props = Astro.props;
import { twMerge } from "tailwind-merge";
---
<button type={props.type} class={twMerge("bg-primary hover:bg-red-700 transition-colors text-white rounded-full px-4 py-4 font-semibold", props.class)}>
  <slot />
</button>